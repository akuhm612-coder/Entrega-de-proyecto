export default function NotFoundPage() {
  return (
    <main className="notfound-shell" style={{ minHeight: 'calc(100vh - 72px)', padding: '4rem 1rem', color: '#fff', background: '#000' }}>
      <h1>Página no encontrada</h1>
      <p>La ruta que intentas visitar no existe. Vuelve al inicio o usa el menú para navegar.</p>
    </main>
  )
}
